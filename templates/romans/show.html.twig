{% extends 'base.html.twig' %}

{% block title %}Roman{% endblock %}

{% block body %}
<div class="container">
    <h1>Roman</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ roman.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ roman.titre }}</td>
            </tr>
            <tr>
                <th>Couverture</th>
                <td>{{ roman.couverture }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ roman.prix }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ roman.genre }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ roman.resume }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('romans_index') }}">
    <button type="button" class="btn btn-outline-info">Retour à la liste</button>
    </a>

    <a href="{{ path('romans_edit', {'id': roman.id}) }}">
     <button type="button" class="btn btn-outline-success mx-3">Editer</button>
    </a>

    {{ include('romans/_delete_form.html.twig') }}
</div>
{% endblock %}
