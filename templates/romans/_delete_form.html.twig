<form method="post" action="{{ path('romans_delete', {'id': roman.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ roman.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
